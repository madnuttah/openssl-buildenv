{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",

  "extends": [
    "config:recommended",
    "config:best-practices",
    ":semanticCommitsDisabled",
    ":automergeStableNonMajor",
    ":automergePatch",
    ":automergeDigest",
    "group:allDigest"
  ],

  "timezone": "Europe/Berlin",

  "regexManagers": [
    {
      "fileMatch": ["^versions/openssl\\.txt$"],
      "matchStrings": ["(?<currentValue>.*)"],
      "datasourceTemplate": "github-tags",
      "depNameTemplate": "openssl/openssl",
      "versioningTemplate": "regex"
    },
    {
      "fileMatch": ["^versions/ngtcp2\\.txt$"],
      "matchStrings": ["(?<currentValue>.*)"],
      "datasourceTemplate": "github-tags",
      "depNameTemplate": "ngtcp2/ngtcp2",
      "versioningTemplate": "regex"
    },
    {
      "fileMatch": ["^versions/nghttp3\\.txt$"],
      "matchStrings": ["(?<currentValue>.*)"],
      "datasourceTemplate": "github-tags",
      "depNameTemplate": "ngtcp2/nghttp3",
      "versioningTemplate": "regex"
    }
  ],

  "packageRules": [
    {
      "matchManagers": ["dockerfile"],
      "enabled": false
    },
    {
      "matchManagers": ["github-actions"],
      "pinDigests": true,
      "automerge": true
    },
    {
      "matchFileNames": ["versions/openssl.txt"],
      "matchDepNames": ["openssl/openssl"],
      "allowedVersions": "<4.0.0"
    }
  ]
}
