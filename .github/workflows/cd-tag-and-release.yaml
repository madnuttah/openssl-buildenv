name: CD Tag And Release

on:
  workflow_run:
    workflows:
      - CD Publish OpenSSL Buildenv Manifests
    types:
      - completed

permissions:
  contents: write
  actions: write
  id-token: write

jobs:
  tag_and_release:
    runs-on: ubuntu-latest
    if: github.event.workflow_run.conclusion == 'success'

    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@f808768d1510423e83855289c910610ca9b43176
        with:
          egress-policy: audit

      - name: Checkout source
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Import GPG key
        id: IMPORT_GPG
        uses: crazy-max/ghaction-import-gpg@2dc316deee8e90f13e1a351ab510b4d5bc0c82cd
        with:
          gpg_private_key: ${{ secrets.BOT_GPG_PRIVATE_KEY }}
          passphrase: ${{ secrets.BOT_GPG_PASSPHRASE }}
          git_user_signingkey: true
          git_commit_gpgsign: true
          git_tag_gpgsign: true
          git_push_gpgsign: false
          trust_level: 5

      - name: Read version files
        id: versions
        run: |
          set -euo pipefail
          OPENSSL_VERSION=$(cat versions/openssl.txt)

          OPENSSL_BE_NONQUIC=$(grep '^openssl=' versions/openssl-buildenv.txt | cut -d= -f2)
          SUB_BE_NONQUIC=$(grep '^sub=' versions/openssl-buildenv.txt | cut -d= -f2)
          NONQUIC_VERSION="${OPENSSL_BE_NONQUIC}-${SUB_BE_NONQUIC}"

          OPENSSL_BE_QUIC=$(grep '^openssl=' versions/openssl-quic-buildenv.txt | cut -d= -f2)
          SUB_BE_QUIC=$(grep '^sub=' versions/openssl-quic-buildenv.txt | cut -d= -f2)
          QUIC_VERSION="${OPENSSL_BE_QUIC}-${SUB_BE_QUIC}"

          NGTCP2_VERSION=$(cat versions/ngtcp2.txt)
          NGHTTP3_VERSION=$(cat versions/nghttp3.txt)

          {
            echo "OPENSSL_VERSION=${OPENSSL_VERSION}"
            echo "NONQUIC_VERSION=${NONQUIC_VERSION}"
            echo "QUIC_VERSION=${QUIC_VERSION}"
            echo "NGTCP2_VERSION=${NGTCP2_VERSION}"
            echo "NGHTTP3_VERSION=${NGHTTP3_VERSION}"
          } >> "$GITHUB_OUTPUT"

      - name: Download SBOM artifacts
        uses: actions/download-artifact@fa0a91b85d4f404e444e00e005971372dc801d16
        with:
          path: sbom-artifacts
        continue-on-error: true

      - name: Push tags and create releases
        env:
          GITHUB_TOKEN: ${{ secrets.BOT_GITHUB_TOKEN }}
          GIT_AUTHOR_NAME: ${{ steps.IMPORT_GPG.outputs.name }}
          GIT_AUTHOR_EMAIL: ${{ steps.IMPORT_GPG.outputs.email }}
          GIT_COMMITTER_NAME: ${{ steps.IMPORT_GPG.outputs.name }}
          GIT_COMMITTER_EMAIL: ${{ steps.IMPORT_GPG.outputs.email }}
        run: |
          set -euo pipefail

          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}"

          OPENSSL="$(printf '%s' "${{ steps.versions.outputs.OPENSSL_VERSION }}")"
          NONQUIC="$(printf '%s' "${{ steps.versions.outputs.NONQUIC_VERSION }}")"
          QUIC="$(printf '%s' "${{ steps.versions.outputs.QUIC_VERSION }}")"
          NGTCP2="$(printf '%s' "${{ steps.versions.outputs.NGTCP2_VERSION }}")"
          NGHTTP3="$(printf '%s' "${{ steps.versions.outputs.NGHTTP3_VERSION }}")"

          TAG_NONQUIC="v${NONQUIC}"
          TAG_QUIC="v${QUIC}-quic"

          git fetch --prune origin --tags

          if ! git rev-parse "${TAG_NONQUIC}" >/dev/null 2>&1; then
            git tag -a "${TAG_NONQUIC}" -m "${TAG_NONQUIC}"
            git push origin "${TAG_NONQUIC}"

            git checkout -b "${NONQUIC}" "${TAG_NONQUIC}"
            git push --set-upstream origin "${NONQUIC}"
          fi

          if ! git rev-parse "${TAG_QUIC}" >/dev/null 2>&1; then
            git tag -a "${TAG_QUIC}" -m "${TAG_QUIC}"
            git push origin "${TAG_QUIC}"

            git checkout -b "${QUIC}-quic" "${TAG_QUIC}"
            git push --set-upstream origin "${QUIC}-quic"
          fi

          NOTES=""

          UPDATED_OPENSSL=false
          UPDATED_NGTCP2=false
          UPDATED_NGHTTP3=false
          UPDATED_QUIC=false

          if ! git diff --quiet HEAD~1 HEAD -- versions/openssl.txt; then
            UPDATED_OPENSSL=true
          fi

          if ! git diff --quiet HEAD~1 HEAD -- versions/ngtcp2.txt; then
            UPDATED_NGTCP2=true
          fi

          if ! git diff --quiet HEAD~1 HEAD -- versions/nghttp3.txt; then
            UPDATED_NGHTTP3=true
          fi

          if ! git diff --quiet HEAD~1 HEAD -- versions/openssl-quic-buildenv.txt; then
            UPDATED_QUIC=true
          fi

          if [ "$UPDATED_OPENSSL" = true ]; then
            NOTES+="## OpenSSL Buildenv\\n"
            NOTES+="OpenSSL: ${OPENSSL}\\n"
            NOTES+="Release notes: https://github.com/openssl/openssl/releases/tag/${OPENSSL#openssl-}\\n"
            NOTES+="Buildenv version: ${NONQUIC}\\n\\n"
          fi

          if [ "$UPDATED_QUIC" = true ] || [ "$UPDATED_NGTCP2" = true ] || [ "$UPDATED_NGHTTP3" = true ]; then
            NOTES+="## OpenSSL QUIC Buildenv\\n"
            NOTES+="Version: ${QUIC}\\n"
            NOTES+="OpenSSL: ${OPENSSL}\\n"

            if [ "$UPDATED_NGTCP2" = true ]; then
              NOTES+="- ngtcp2: ${NGTCP2}\\n"
              NOTES+="  Release notes: https://github.com/ngtcp2/ngtcp2/releases/tag/${NGTCP2}\\n"
            fi

            if [ "$UPDATED_NGHTTP3" = true ]; then
              NOTES+="- nghttp3: ${NGHTTP3}\\n"
              NOTES+="  Release notes: https://github.com/ngtcp2/nghttp3/releases/tag/${NGHTTP3}\\n"
            fi

            NOTES+="\\n"
          fi

          printf "%b" "$NOTES" > RELEASE_NOTES.md

          gh release create "${TAG_NONQUIC}" \
            -t "${TAG_NONQUIC}" \
            -F RELEASE_NOTES.md \
            --latest || true

          gh release create "${TAG_QUIC}" \
            -t "${TAG_QUIC}" \
            -F RELEASE_NOTES.md \
            --latest=false || true
            
            .github\workflows\cd-tag-and-release.yaml
            
          # cosign attest --yes \
            # --predicate-type slsaprovenance.dev/v1 \
            # --predicate <(echo '{"buildType":"git-tag","tag":"v'"${NONQUIC_VERSION}"'"}') \
            # --tag-only "v${NONQUIC_VERSION}" \
            # "ghcr.io/${{ github.repository }}" || true
          
          # cosign attest --yes \
            # --predicate-type slsaprovenance.dev/v1 \
            # --predicate <(echo '{"buildType":"git-tag","tag":"v'"${QUIC_VERSION}"'-quic"}') \
            # --tag-only "v${QUIC_VERSION}-quic" \
            # "ghcr.io/${{ github.repository }}" || true

      - name: Prepare release artifacts
        run: |
          set -euo pipefail

          mkdir -p release-artifacts

          if [ -d sbom-artifacts ]; then
            find sbom-artifacts -type f \( -name "*.json" -o -name "*.cyclonedx.json" \) -print0 | \
            while IFS= read -r -d '' sbom; do
              cp "$sbom" release-artifacts/ || true
            done
          fi

          cd release-artifacts
          if [ -n "$(ls -A)" ]; then
            find . -type f ! -name "*.sha256" -exec sha256sum {} \; > CHECKSUMS.sha256
          fi
          cd -

      - name: Upload release artifacts to releases
        env:
          GITHUB_TOKEN: ${{ secrets.BOT_GITHUB_TOKEN }}
        run: |
          set -euo pipefail

          NONQUIC_VERSION="${{ steps.versions.outputs.NONQUIC_VERSION }}"
          QUIC_VERSION="${{ steps.versions.outputs.QUIC_VERSION }}"

          if [ -d release-artifacts ] && [ -n "$(ls -A release-artifacts)" ]; then
            cd release-artifacts

            for file in sbom-openssl-buildenv-*; do
              [ -f "$file" ] && gh release upload "v${NONQUIC_VERSION}" "$file" --clobber || true
            done

            for file in sbom-openssl-quic-buildenv-*; do
              [ -f "$file" ] && gh release upload "v${QUIC_VERSION}-quic" "$file" --clobber || true
            done

            cd -
          fi

      # - name: Install Cosign
      #   uses: sigstore/cosign-installer@v3
      #   with:
      #     cosign-release: 'v2.4.0'

      # - name: Sign Git tags with Cosign
      #   env:
      #     COSIGN_EXPERIMENTAL: "1"
      #     NONQUIC_VERSION: ${{ steps.versions.outputs.NONQUIC_VERSION }}
      #     QUIC_VERSION: ${{ steps.versions.outputs.QUIC_VERSION }}
      #   run: |
      #     set -euo pipefail
      #
      #     cosign attest --yes \
      #       --predicate-type slsaprovenance.dev/v1 \
      #       --predicate <(echo '{"buildType":"git-tag","tag":"v'"${NONQUIC_VERSION}"'"}') \
      #       --tag-only "v${NONQUIC_VERSION}" \
      #       "ghcr.io/${{ github.repository }}" || true
      #
      #     cosign attest --yes \
      #       --predicate-type slsaprovenance.dev/v1 \
      #       --predicate <(echo '{"buildType":"git-tag","tag":"v'"${QUIC_VERSION}"'-quic"}') \
      #       --tag-only "v${QUIC_VERSION}-quic" \
      #       "ghcr.io/${{ github.repository }}" || true
